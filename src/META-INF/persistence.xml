<?xml version="1.0" encoding="UTF-8"?>

<persistence version="2.1" xmlns="http://xmlns.jcp.org/xml/ns/persistence"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://xmlns.jcp.org/xml/ns/persistence
			http://xmlns.jcp.org/xml/ns/persistence/persistence_2_1.xsd">

	<!-- * 영속성 유닛 설정 : 여기 지정한 name값으로 찾음 -->
	<persistence-unit name="aBasic"> <!-- 이름 안써도 되는데 헷갈리니까 쓰기  -->
	
	<!-- JAP (DB) 프라퍼티 설정  -->
	<properties>
		<property name="javax.persistence.jdbc.driver" value="org.mariadb.jdbc.Driver"/>
		<property name="javax.persistence.jdbc.user" value="root"/>
		<property name="javax.persistence.jdbc.password" value="admin1234"/>
		<property name="javax.persistence.jdbc.url" value="jdbc:mariadb://127.0.0.1:3306/test?characterEncoding=utf8"/>
		
		<!-- JPA 구현체 설정 -->
		<property name="hibernate.dialect" value="org.hibernate.dialect.MariaDB103Dialect"/>
		
		<!-- 옵션 -->
		<property name="hibernate.show_sql" value="true"/> <!-- 콘솔에 sql 진행이 보임 -->
		<property name="hibernate.hbm2ddl.auto" value="update"/> 
		<!-- create : ddl / 실행하는 순간에 테이블이 자동으로 생성됨 안만들어질 경우 데이터베이스가서 만들면됨
		create할 경우 main에서 데이터를 입력 한 후에 또 입력하면 기존에 있던 정보가 전부 드랍 된 후 들어감 
		그래서 수업 중에는 create안쓰고 update 쓸것 -->
		<!-- update : crud / DeptVO의 @Table에(name="")이름 지정했을 경우 데이터베이스쪽에 없는 테이블 이름이면 테이블이 자동으로 생성됨-->
		
	</properties>

	</persistence-unit>

</persistence>